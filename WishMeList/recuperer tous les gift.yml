info:
  name: recuperer tous les gift
  type: http
  seq: 7

http:
  method: GET
  url: http://localhost:8080/gifts
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
