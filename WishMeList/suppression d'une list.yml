info:
  name: "suppression d'une list "
  type: http
  seq: 12

http:
  method: DELETE
  url: http://localhost:8080/api/wishlists/102
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
