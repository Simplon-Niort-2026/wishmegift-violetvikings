info:
  name: modification une seul info user
  type: http
  seq: 3

http:
  method: PATCH
  url: http://localhost:8080/api/auth/1
  body:
    type: json
    data: |-
      {
        "username": "Test"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
