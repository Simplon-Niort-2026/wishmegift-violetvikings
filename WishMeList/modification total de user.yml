info:
  name: modification total de user
  type: http
  seq: 4

http:
  method: PUT
  url: http://localhost:8080/api/auth/1
  body:
    type: json
    data: |-
      {
        "username": "Test",
        "email": "viking_new@test.com",
        "password": "NouveauMdpTresFort2026@"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
