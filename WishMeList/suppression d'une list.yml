info:
  name: "suppression d'une list "
  type: http
  seq: 2

http:
  method: DELETE
  url: http://localhost:8080/API/wishlists/1
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
