info:
  name: delete un user
  type: http
  seq: 13

http:
  method: DELETE
  url: http://localhost:8080/api/auth/1
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
