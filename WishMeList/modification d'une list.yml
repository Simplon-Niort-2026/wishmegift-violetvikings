info:
  name: modification d'une list
  type: http
  seq: 4

http:
  method: PUT
  url: http://localhost:8080/api/wishlists/102
  body:
    type: json
    data: |-
      {
        "name": "Liste de Noël (Mise à jour)",
        "theme": "NOEL",
        "description": "Nouvelle description pour la présentation",
        "author": "Test"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
