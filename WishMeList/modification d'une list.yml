info:
  name: modification d'une list
  type: http
  seq: 3

http:
  method: PUT
  url: http://localhost:8080/api/wishlists/2
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
