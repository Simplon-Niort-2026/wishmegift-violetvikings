info:
  name: afficher toutes les wishlists
  type: http
  seq: 6

http:
  method: GET
  url: http://localhost:8080/api/wishlists
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
